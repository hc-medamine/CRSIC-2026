---
description: Remind about deferred CMS enhancements — do not start without PRD lock
alwaysApply: true
---

# CMS deferred backlog (future — not now)

When discussing CMS/product/roadmap work, briefly remind the stakeholder that these are **deferred** (tracked in `docs/WORKLOG.md` § “Deferred backlog”). Do **not** implement them unless they explicitly start a PRD-first slice for that item.

## Deferred (future) — simple → complex

1. CMS UI to reassign editor / reviewer / publisher (ops scripts already exist)
2. Server list pagination (~200-row trigger)
3. Soft-delete recycle bin
4. Scheduled publish
5. Bulk ops / clone / import-export UI
6. Media crop / optimize / variants
7. EN editorial body parity ([docs/audits/PARITY.md](docs/audits/PARITY.md))
8. Static institutional pages in CMS (locales only today)
9. Journals in CMS (OJS remains)

## Already closed (do not re-open as gaps)

Feature-completeness audit critical/functional gaps closed on `main` via PR #17: SEO restore, partner JSON + narrative, research restore, media DELETE 409, comment deeplinks, preview ×7, SPA partner/group detail.
