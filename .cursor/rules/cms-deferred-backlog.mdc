---
description: Remind about deferred CMS enhancements — do not start without PRD lock
alwaysApply: true
---

# CMS deferred backlog (future — not now)

When discussing CMS/product/roadmap work, briefly remind the stakeholder that these are **deferred** (tracked in `docs/WORKLOG.md` § “Deferred backlog”). Do **not** implement them unless they explicitly start a PRD-first slice for that item.

## Active Approved PRD (this slice — implement)

- CMS Desk production boost — [docs/prds/2026-08-24-cms-desk-production-boost.md](docs/prds/2026-08-24-cms-desk-production-boost.md) (**Approved** 2026-08-24). SA JSON zip + cover crop/card variant + EN show-when-ready. One build, one walk.

## Deferred (future) — simple → complex

Do **not** implement these unless they explicitly start a PRD-first slice.

1. Static institutional pages in CMS (locales only today)
2. Journals in CMS (OJS remains)

## Already closed (do not re-open as gaps)

- **Scheduled publish — cancelled** (2026-07-21, confirmed 2026-08-22). Manual Approve → Publish only. No `scheduled` status, cron, or schedule UI. Do not add it back to the deferred list.
- CMS Recycle bin (PRD 2026-08-22): SA bin unpublished/rejected; restore to draft; purge unused media. On `main` (PR #34).
- CMS Editor recycle (PRD 2026-08-23): Editors bin own draft/rejected; scoped bin + restore. On `main` (PR #41).
- CMS clone Cut 1 (PRD 2026-08-22): Duplicate → new draft (edit + list row; bulk Reviewer/SA). On `main` (PR #42).
- CMS table header sort (PRD 2026-08-24): click data headers; this visit only; no URL `?sort=`. On `main` (PR #43).
- CMS news list bulk (PRD 2026-08-22): unpublish + SA recycle; skip + report. On `main` (PR #36).
- CMS list bulk for remaining types (PRDs 2026-08-22): events, publications, partners, alerts, laws, platforms, research groups, research projects. On `main` (PR #37).
- CMS list Load more (PRD 2026-08-22): news/events/publications page size 20.
- Align authorship / Desks (PRD 2026-08-22): claims + Align, assignable publisher, JSON rebuild.
- Feature-completeness audit critical/functional gaps closed on `main` via PR #17: SEO restore, partner JSON + narrative, research restore, media DELETE 409, comment deeplinks, preview ×7, SPA partner/group detail.
