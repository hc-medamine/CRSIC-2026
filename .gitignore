# OS
Thumbs.db
Desktop.ini
.DS_Store
ehthumbs.db

# Editors / IDE
.vscode/*
!.vscode/settings.json
*.swp
*.swo
*~

# Local / AI tooling (not part of the public site)
.claude/

# Env / secrets (none used yet; ignore if added later)
.env
.env.*
!.env.example

# CMS publish backups / temp artifacts of public JSON
data/*.json.bak
data/*.json.smoke-snap
data/*.json.tmp

# CMS local upload staging (not published yet).
# Published public media under img/cms/ is tracked in git.
cms/uploads/**
!cms/uploads/.gitkeep

# Logs & caches
*.log
.cache/
tmp/
temp/

# Optional Node tooling (root package.json is convenience-only; cms/ has its own deps)
node_modules/
dist/
build/
.serve/
.next/
out/
